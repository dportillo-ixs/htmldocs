---
"htmldocs": patch
---

Updated the starter template (used by the init command) so that all dependency and script references use the 'htmldocs-v2' package instead of 'htmldocs'. The generated starter project now correctly depends on htmldocs-v2.
